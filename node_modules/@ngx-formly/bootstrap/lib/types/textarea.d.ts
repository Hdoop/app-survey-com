import { FieldType } from '@ngx-formly/core';
export declare class FormlyFieldTextArea extends FieldType {
    defaultOptions: {
        templateOptions: {
            cols: number;
            rows: number;
        };
    };
}
